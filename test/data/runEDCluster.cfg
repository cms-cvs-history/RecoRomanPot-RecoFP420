process CluFP420Test = {
 ####### include services
### include "Configuration/ReleaseValidation/data/Services.cfi"

  ########## initialize  MessageLogger
 # include "FWCore/MessageLogger/data/MessageLogger.cfi"

   include "FWCore/MessageService/data/MessageLogger.cfi"
   replace MessageLogger.cerr.default.limit = 10




   service = RandomNumberGeneratorService
   {
      untracked uint32 sourceSeed = 123456789
      PSet moduleSeeds =
      {
         untracked uint32 VtxSmeared = 98765432
	 untracked uint32 g4SimHits  = 11
	 untracked uint32 mix        = 12345   
      }
   }

        untracked PSet maxEvents = {untracked int32 input = 200}
############################################################
source = PoolSource {
# see                                                             in  ~/scratch0/CMSSW_1_0_6/src/Analysis
        untracked vstring fileNames = {"file:testDIevent.root"}
#       untracked vstring fileNames = {'file:./testDIevent.root'}
     untracked uint32 debugVebosity = 100
     untracked bool   debugFlag     = true
}
############################################################

# Initialize geometry with XML files:
	#Geometry
        include "Geometry/FP420CommonData/data/FP420GeometryXML.cfi"

# Initialize magnetic field: 
	#Magnetic Field
#	include "MagneticField/Engine/data/volumeBasedMagneticField.cfi"
# You can set Mag Field to 0
#
#include "Configuration/StandardSequences/data/UseZeroBField.cff"



# Seeds end
#


#  include "SimGeneral/MixingModule/data/mixNoPU.cfi"
# include file for zero adding of pileup


#  include "SimRomanPot/SimFP420/data/FP420Digi.cfi"
  include "RecoRomanPot/RecoFP420/data/FP420Cluster.cfi"
#
###############################################   
       module o1 = PoolOutputModule {untracked string fileName ="testCLevent.root"} 
   path p1 = {  FP420Cluster }
#   path p1 = {  VtxSmeared, g4SimHits, mix, FP420Digi , FP420Cluster }

  schedule = {p1, outpath}
 
	endpath outpath = { o1 }

	service = Timing {}
	service = Tracer {}
}		
###############################################   
